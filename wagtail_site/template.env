# Generate a Django Secret key with:
# poetry run python manage.py shell
# >>> from django.core.management.utils import get_random_secret_key
# >>> get_random_secret_key()
DJANGO_SECRET=

# Specifies the domain name at which this site will be hosted
# as a comma delimited list
ALLOWED_HOSTS=

# The settings module to use. Set to `wagtail_site.settings.production` in prod.
DJANGO_SETTINGS_MODULE=wagtail_site.settings.dev

# DB settings
DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=
DB_NAME=
# Set to `verified` for prod or `allowed` for local development
DB_SSL_MODE=
